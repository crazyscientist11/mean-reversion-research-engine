# Prediction journal

Prediction snapshots belong in a configurable local directory (the default is `predictions/private/`, which Git ignores). A snapshot records the model's information set at a cutoff and is never edited to include later observations. Evaluations are separate JSON records.

